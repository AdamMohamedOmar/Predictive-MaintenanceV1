# Python 3.11 required.
#
# These version pins are chosen to be mutually compatible and to have
# precompiled wheels for Windows (critical - we do not want to compile
# anything from source on Windows laptops).
#
# If any pin breaks on install, loosen it to a minor-version range and
# note the change in docs/DATA_NOTES.md. Do not silently upgrade major
# versions - that has broken APIs in this stack before.

# Core data handling
pandas==2.2.3
numpy==1.26.4

# Classical ML
scikit-learn==1.5.2
xgboost==2.1.3

# NOTE: If xgboost + scikit-learn raise an AttributeError about
# "sklearn_tags" when using GridSearchCV, this is a known compatibility
# issue. Workaround: pin xgboost==2.0.3 with scikit-learn==1.4.2, or
# upgrade both together to the latest and re-test. Do not mix a new
# xgboost with an old sklearn or vice versa.

# Deep learning
torch==2.3.1

# Explainability
shap==0.46.0

# OBD-II live reading
obd==0.7.2
pyserial==3.5

# Dashboard
streamlit==1.38.0
plotly==5.24.1              # Radial gauges and dark-themed sparklines for diagnostic UI

# Model export
onnx==1.16.2
onnxruntime==1.18.1
skl2onnx==1.17.0         # XGBoost and sklearn -> ONNX conversion

# Testing
pytest==8.3.3

# Notebooks
jupyter==1.1.1
matplotlib==3.9.2
seaborn==0.13.2

# Utilities
tqdm==4.66.5
